---
description: Initialize Megaphone for the current repo (scans project, writes .megaphone/profile.json)
---

Invoke the `megaphone-init` skill on the current working directory.

Pass through any user arguments as additional context: $ARGUMENTS
