---
description: Audit landing page (100-pt rubric) and/or user journey (70-pt + three-persona walkthrough)
---

Invoke the `megaphone-audit` skill.

Audit type and target URL (if specified): $ARGUMENTS

If no target URL is in the profile or arguments, ask the user for one before proceeding.
